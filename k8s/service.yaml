apiVersion: v1
kind: Service
metadata:
  name: k8s-demo-app
  labels:
    app: k8s-demo-app
spec:
  type: NodePort
  selector:
    app: k8s-demo-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
